# See AGENTS.md

Follow the instructions in `AGENTS.md`.

Read `PROJECT_CONTEXT.md` before starting non-trivial work. It is the project
handover document: architecture, decisions already made, the monorepo layout
(`frontend/` + `backend/`), the boundary between the React app and the Python
service, and the open questions that must not be answered unilaterally.
